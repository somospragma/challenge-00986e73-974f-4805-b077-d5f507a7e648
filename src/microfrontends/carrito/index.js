import React from 'react';
import ReactDOM from 'react-dom';

const carritoApp = () => {
  const domElement = document.getElementById('single-spa-application:carrito');
  ReactDOM.render(<div>Carrito Microfrontend</div>, domElement);
};

export default carritoApp;